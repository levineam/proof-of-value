{
  "name": "@pov/protocol",
  "version": "0.0.0",
  "private": true,
  "description": "Shared, dependency-free TypeScript runtime contracts for Swarm facts.",
  "type": "module",
  "exports": "./dist/index.js",
  "scripts": { "build": "tsc -p tsconfig.json", "test": "npm run build && node --test test/*.test.mjs" }
}
